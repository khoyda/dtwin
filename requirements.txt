# Runtime deps for the forecast app + web UI (used by the Vercel deploy).
# This is intentionally the SLIM runtime subset -- the forecasting/advisory/UI path does
# NOT use scikit-learn/joblib (those are only for the optional ML training in
# canola_dt.training). For full local dev incl. the ML model: pip install -e ".[dev]"
numpy>=1.26
pandas>=2.1
pyyaml>=6.0
matplotlib>=3.8
flask>=3.0
